import React from "react";
import { StyleSheet, Text, View, Button } from "react-native";

const Welcome = () => {
  return (
    <View>
      <Text>Welcome to Adivina</Text>
    </View>
  );
};

export default Welcome;

const styles = StyleSheet.create({});
